# Archivos de referencia (no usados por el contenedor)

- **nginx.conf** – Copia de la config Nginx anterior; el contenedor usa `nginx.conf.template` (raíz del proyecto).
- **stunnel.conf** – Config Stunnel con host fijo; el contenedor usa `stunnel.conf.template` y genera la config en tiempo de ejecución.

Puedes borrar esta carpeta si no los necesitas.
