# Archivos de referencia (no usados por el contenedor)

- **nginx.conf** – Copia de la config Nginx anterior; el contenedor usa `templates/nginx.conf.template`.
- **stunnel.conf** – Config Stunnel con host fijo; el contenedor usa `templates/stunnel.conf.template` y genera la config en tiempo de ejecución.

Puedes borrar esta carpeta si no necesitas estas copias de referencia.
